name: Build and push images
on:
  push:
    tags:
      - '*'

jobs:
  build-and-push:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v2

      - name: Login to DockerHub
        uses: docker/login-action@v1 
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Docker meta
        id: meta
        uses: crazy-max/ghaction-docker-meta@v2
        with:
          images: |
            daemon63/fetchd
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=fetchhub
            
      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          target: fetchd
          build-args:
            - DEFAULT_RPC_ENDPOINT="https://rpc-fetchhub.fetch-ai.com:443"
            - DEFAULT_SEEDS="9f9774d88bb6ff9f43b395baf0e7a4baba27dec6@connect-fetchhub.m-v2-london-c.fetch-ai.com:36856"

      # - name: Setup GCloud
      #   uses: GoogleCloudPlatform/github-actions/setup-gcloud@master
      #   with:
      #     service_account_key: ${{ secrets.GCLOUD_KEY_IMAGES }}

      # - name: Login to GCR Registry
      #   run: |
      #     gcloud auth configure-docker

      # - name: Build and Publish Private Images
      #   uses: ejfitzgerald/action-docker-tag@v0.2.0-alpha2
      #   with:
      #     repo: gcr.io/fetch-ai-images/fetchd
      #     target: gcr
